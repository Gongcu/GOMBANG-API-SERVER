definitions:
   user_response:
    type: object
    properties:
      _id:
        type: string
        description: (기본 생성)
      kakaoId:
        type: string
        description: 카카오 고유 ID
      token:
        type: string
        description: FCM 토큰
      login:
        type: boolean
        description: 로그인 여부
      name:
        type: string
        description: 이름
      image:
        type: string
        description: 이미지 이름
      email:
        type: string
        description: 이메일 주소
      phone:
        type: string
        description: 핸드폰 번호
      birth:
        type: string
        description: 이메일 주소
      student_number:
        type: string
        description: 이메일 주소
      college:
        type: string
        description: 대학
      department:
        type: string
        description: 학과
      nickname:
        type: string
        description: 동아리, 닉네임 one of
      signed_club_list:
        type: array
        items:
          $ref: '#/definitions/signed_club_list'
      favorite_club_list:
        type: array
        items:
          $ref: '#/definitions/favorite_club_list'
        
         
        